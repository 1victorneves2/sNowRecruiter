{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "npx prisma generate && npx prisma db push --accept-data-loss && node src/app.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 3
  }
}
